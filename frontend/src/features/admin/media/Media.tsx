import React from 'react';
import { Box } from '@mui/material';

const Media: React.FC = () => {
  return (
    <Box
        className="app-page"
        display='flex'
        flexDirection='row'
        width='100%'
        p={2}
    >
      Media
    </Box>
  );
};

export default Media;
