import React from 'react';
import { Box } from '@mui/material';

const Dashboard: React.FC = () => {
  return (
    <Box
        className="app-page"
        display='flex'
        flexDirection='row'
        width='100%'
        p={4}
    >
      Dashboard
    </Box>
  );
};

export default Dashboard;
