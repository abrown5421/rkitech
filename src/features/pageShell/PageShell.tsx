import React from 'react';
import Container from '../../shared/components/container/Container';

const PageShell: React.FC = () => {

    return (
        <Container padding='md' className='bg-white z-20 h-[calc(100vh-50px)]' >
            page
        </Container>
    );
};

export default PageShell;
