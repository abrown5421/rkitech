import {
  Box,
  Typography,
  Tooltip,
  Stack,
  Button,
  Paper,
  Card
} from "@mui/material";

export const componentMap: Record<string, React.ElementType> = {
  box: Box,
  typography: Typography,
  tooltip: Tooltip,
  stack: Stack,
  button: Button,
  paper: Paper,
  card: Card,
};
