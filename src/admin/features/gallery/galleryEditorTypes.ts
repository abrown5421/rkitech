export type NewImageFormValues = {
  imageName: string;
  imageDescription: string;
  urlInput: string;
};

export type NewImageFormErrors = {
  imageName: boolean;
  imageDescription: boolean;
  urlInput: boolean;
};

export type NewImageFormHelperTexts = {
  imageName: string;
  imageDescription: string;
  urlInput: string;
};